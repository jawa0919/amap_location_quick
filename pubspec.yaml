name: amap_location_quick
description: A new flutter plugin project.
version: 0.0.1
homepage: https://github.com/jawa0919/amap_location_quick

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.example.amap_location_quick
        pluginClass: AmapLocationQuickPlugin
      ios:
        pluginClass: AmapLocationQuickPlugin
